# 수업자료의 장인 - UI 스켈레톤 프로젝트

초등학교 3학년 사회과 지역화 교과서 수업자료 자동 생성 서비스의 UI 스켈레톤입니다.

## 🚀 프로젝트 구조

```
project1/
├── app/
│   ├── layout.tsx              # 루트 레이아웃 (Header 포함)
│   ├── page.tsx                # 메인 페이지 (서비스 소개)
│   ├── globals.css             # Tailwind + shadcn/ui 스타일
│   ├── regions/
│   │   └── page.tsx            # 지역 선택 페이지
│   ├── generate/
│   │   └── page.tsx            # PPT 생성 페이지
│   ├── downloads/
│   │   └── page.tsx            # 다운로드 페이지
│   └── admin/
│       ├── layout.tsx          # 관리자 레이아웃
│       ├── templates/
│       │   └── page.tsx        # 템플릿 관리 페이지
│       └── stats/
│           └── page.tsx        # 통계 페이지
├── components/
│   ├── layout/
│   │   ├── Header.tsx          # 상단 헤더 (교사/관리자 전환)
│   │   ├── Navigation.tsx      # 교사용 좌측 네비게이션
│   │   └── AdminNavigation.tsx # 관리자용 좌측 네비게이션
│   ├── regions/
│   │   ├── RegionSearch.tsx    # 지역 검색 (use client)
│   │   └── RegionCard.tsx      # 지역 카드 (use client)
│   ├── generate/
│   │   ├── GenerateButton.tsx      # 생성 버튼 (use client)
│   │   └── GenerationStatus.tsx    # 생성 상태 표시 (use client)
│   └── admin/
│       ├── TemplateCard.tsx    # 템플릿 카드
│       ├── TemplateForm.tsx    # 템플릿 등록 폼 (use client)
│       └── StatsCard.tsx       # 통계 카드
├── lib/
│   ├── dummy-data.ts           # 더미 데이터 (지역, 템플릿, 통계)
│   └── utils.ts                # cn() 유틸리티
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── postcss.config.mjs
└── next.config.mjs
```

## 📋 주요 기능 (UI만 구현됨)

### 교사 모드
- **메인 페이지** (`/`): 서비스 소개, 주요 통계, 바로 시작하기 버튼
- **지역 선택** (`/regions`): 검색 기능이 있는 지역 카드 목록
- **PPT 생성** (`/generate`): 선택된 지역 정보 표시 + 생성 버튼 + 상태 표시
- **다운로드** (`/downloads`): 생성된 PPT 목록 + 다운로드 버튼

### 관리자 모드
- **템플릿 관리** (`/admin/templates`): 템플릿 목록, 등록 폼, 수정/삭제 버튼
- **통계** (`/admin/stats`): KPI 카드, 인기 지역 TOP 5, 월별 추이 차트

## 🎨 디자인 원칙

1. **Web-first**: 브라우저 기반, 데스크탑 최적화
2. **Simple & Familiar**: 교사 친화적인 단순 UI
3. **Server Component 우선**: 클라이언트 인터랙션이 필요한 부분만 'use client'
4. **TailwindCSS + shadcn/ui**: 폼, 카드, 모달에만 shadcn 사용

## 🛠️ 기술 스택

- **Framework**: Next.js 14 (App Router)
- **Styling**: TailwindCSS + shadcn/ui
- **Language**: TypeScript
- **Icons**: lucide-react
- **Validation**: Zod
- **Security**: DOMPurify
- **Testing**: Jest + React Testing Library

## 📦 설치 및 실행

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev

# 테스트 실행
npm test

# 테스트 watch 모드
npm run test:watch

# 테스트 커버리지
npm run test:coverage

# 린트 실행
npm run lint

# 빌드
npm run build
```

브라우저에서 `http://localhost:3000` 접속

## 🔒 보안 및 검증

### 입력 검증 (Zod)
- 템플릿 폼 검증 (제목, 설명, 카테고리, 슬라이드 수)
- 파일 업로드 검증 (크기 제한 50MB, PPTX 형식만 허용)
- 지역 검색 검증
- PPT 생성 옵션 검증

### XSS 방지 (DOMPurify)
- HTML 입력 새니타이징
- 텍스트 입력 새니타이징
- 파일명 새니타이징 (경로 탐색 공격 방지)
- URL 검증 (javascript:, data: 프로토콜 차단)
- 검색 쿼리 새니타이징 (SQL Injection 방지)

### Rate Limiting (클라이언트 사이드)
- API 요청: 분당 30회
- PPT 생성: 분당 5회
- 검색: 분당 20회
- 버튼 쿨다운: 500ms
- 폼 제출 쿨다운: 2초

### 보안 헤더 (next.config.mjs)
- Strict-Transport-Security (HSTS)
- X-Frame-Options (클릭재킹 방지)
- X-Content-Type-Options (MIME 스니핑 방지)
- X-XSS-Protection
- Content-Security-Policy (CSP)
- Referrer-Policy
- Permissions-Policy

## ✅ 테스트

### 테스트 커버리지
- **유닛 테스트**: 유틸리티 함수, 보안 함수, 에러 핸들러
- **통합 테스트**: API 클라이언트, Rate Limiting

### 테스트된 기능
- 한글 초성 검색
- 데이터 내보내기 (CSV, PDF)
- HTML/텍스트 새니타이징
- 파일명 검증
- URL 검증
- Rate Limiting
- 에러 핸들링
- API 요청/응답 처리

```bash
# 테스트 실행
npm test

# 110개 이상의 테스트가 통과해야 합니다
Test Suites: 6 passed, 6 total
Tests:       110 passed, 110 total
```

## ⚠️ 주의사항

- 현재는 **UI 스켈레톤과 보안/검증 레이어**가 구현되어 있습니다.
- 실제 백엔드 기능(DB 연동, 실제 파일 다운로드)은 구현되지 않았습니다.
- 더미 데이터는 `lib/dummy-data.ts`에 있습니다.
- localStorage를 사용한 간단한 상태 저장만 구현되어 있습니다.

## 📝 향후 실제 기능 연결 시 수정할 파일 목록

### 백엔드 API 연결 필요
1. **`app/regions/page.tsx`**
   - 지역 목록 API 호출로 교체
   - 선택된 지역을 상태관리 라이브러리로 저장

2. **`app/generate/page.tsx`**
   - PPT 생성 API 호출 (`POST /api/generate`)
   - WebSocket을 통한 실시간 생성 상태 업데이트

3. **`app/downloads/page.tsx`**
   - 생성 기록 조회 API (`GET /api/files`)
   - 실제 파일 다운로드 엔드포인트 연결

4. **`app/admin/templates/page.tsx`**
   - 템플릿 CRUD API 연결
   - 파일 업로드 API (`POST /api/templates`)

5. **`app/admin/stats/page.tsx`**
   - 통계 데이터 API 호출 (`GET /api/stats`)
   - 실시간 차트 라이브러리 통합 (recharts 등)

### 삭제/교체 필요
- **`lib/dummy-data.ts`**: API로 완전히 교체
- localStorage 로직: 상태관리 라이브러리(Zustand, Redux 등)로 교체

### 추가 구현 필요
- 인증/로그인 기능 (교사/관리자 구분)
- 에러 바운더리 및 로딩 상태
- Toast 알림 (react-hot-toast 등)
- 파일 다운로드 진행률 표시
- 반응형 디자인 개선 (모바일)

## 📂 컴포넌트 구조 요약

### Server Components (기본)
- 모든 페이지 (`page.tsx`)
- 레이아웃 컴포넌트
- 정적 UI 컴포넌트

### Client Components ('use client')
- `RegionSearch.tsx` - 검색 입력
- `RegionCard.tsx` - 카드 클릭 이벤트
- `GenerateButton.tsx` - 생성 버튼 클릭
- `GenerationStatus.tsx` - 상태 변화 표시
- `TemplateForm.tsx` - 폼 입력 처리

## 🎯 PRD 반영 사항

- ✅ 지역 선택 → 자동 생성 → 다운로드 플로우
- ✅ 관리자 템플릿 관리 및 통계 대시보드
- ✅ Web-first, Simple & Familiar 디자인
- ✅ 좌측 네비게이션 + 상단 헤더 레이아웃
- ✅ 한글 UI 텍스트
- ✅ 카드 기반 정보 표시

## 📚 문서

추가 문서는 [`docs/`](./docs) 폴더를 참고하세요:

- [Supabase 설정 가이드](./docs/SUPABASE_SETUP.md) - Supabase 프로젝트 설정 및 연동
- [Storage 설정 가이드](./docs/STORAGE_SETUP.md) - 파일 스토리지 설정
- [이미지 API 설정 가이드](./docs/IMAGE_API_SETUP.md) - Unsplash, Pixabay API 설정
- [템플릿 작성 가이드](./docs/TEMPLATE_GUIDE.md) - PPTX 템플릿 placeholder 규칙

**프로젝트 스펙**: [spec.md](./spec.md) 참고

## 📞 문의

프로젝트 관련 문의사항은 이슈를 등록해주세요.
